part of '../controller/setting.dart';

class SettingView extends StatelessView<SettingScreen, SettingController> {
  const SettingView(SettingController state, {Key? key})
      : super(state, key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
